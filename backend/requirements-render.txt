# Render-only extras. Pulls in the shared deps from requirements.txt plus the
# Turso SQLAlchemy dialect. sqlalchemy-libsql has no Windows wheels (its
# libsql-experimental dep ships macOS/Linux only), so it stays out of
# requirements.txt or local Windows setup would try to build it from source.
-r requirements.txt
sqlalchemy-libsql==0.2.0
